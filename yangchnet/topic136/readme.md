# 只出现一次的数字

## 1. 题目描述
给定一个非空整数数组，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。

说明：

你的算法应该具有线性时间复杂度。 你可以不使用额外空间来实现吗？

## 2. 示例
示例1
```
输入: [2,2,1]
输出: 1
```

示例2
```
输入: [4,1,2,1,2]
输出: 4
```

## 3. 解题
使用位运算来做  
根据异或运算的性质，a^a = 0，因此将所有数字做异或后，剩下的数字就是只出现一次的数字。